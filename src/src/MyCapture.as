package  {		import flash.display.MovieClip;	import flash.events.Event;		public class MyCapture extends Capture {				var item1:SmoothingBitmapLoader=null;		public function MyCapture() {			// constructor code			super();						//getFullPath(this.root.loaderInfo);			//		}		override protected function init()		{			super.init();			//item1 = new SmoothingBitmapLoader(getRootPath()+"./data/item.png");			Pause();			//item1.addEventListener(SmoothingBitmapLoader.INIT,function onComplete(e:Event)//								   {//									  Start(); //								   });		}		override protected function onDraw()		{			super.onDraw();			//if(item1!=null && bitmapData!=null)bitmapData.draw(item1);		}	}	}